<h1>projekt.</h1>
<h2><%=h @project.name %></h2>

<p>
  <%=h @project.description %>
</p>

<%= render :partial => 'actions/list', :locals => {:project => @project} %>
<%= render :partial => 'actions/create', :locals => {:project => @project} %>


<%= link_to_function 'add action', "$('attach_action').toggleClassName('pleasehide'); $('attach_action_status').innerHTML='';"  %> |

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Obliterate', @project, :confirm => 'ya, rly?', :method => :delete %> |
<%= link_to 'Back', projects_path %>
